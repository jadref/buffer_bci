package nl.dcc.buffer_bci.imaginedMovement.views;

import nl.dcc.buffer_bci.imaginedMovement.State;
import org.apache.commons.configuration.Configuration;

/**
 * Plain panel.
 * @author Bas Bootsma
 */
public class PlainPanel extends BasePanel
{
    public PlainPanel(Configuration configuration, State state)
    {
        super(configuration, state);
    }
}
